# /home/ubuntu/backend/AuthService/nginx/conf.d/upstream.conf

# Single-instance services that do not require load balancing
upstream client_backend {
    server client-service:80 max_fails=3 fail_timeout=30s;
    keepalive 32;
}
