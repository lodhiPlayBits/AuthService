# ── Client Environment Variables ─────────────────────────────────
# Vite exposes env vars prefixed with VITE_ to the browser.
# Change VITE_API_BASE_URL to match your deployment.

# API base URL (relative path works when served behind the same Nginx gateway)
VITE_API_BASE_URL=/api/v1
